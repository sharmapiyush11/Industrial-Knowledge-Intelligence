{
  "name": "industrial-knowledge-intelligence",
  "version": "1.0.0",
  "description": "Unified Asset & Operations Knowledge Intelligence Platform",
  "scripts": {
    "dev:frontend": "npm --prefix frontend run dev",
    "dev:backend": "uvicorn main:app --app-dir backend --host 127.0.0.1 --port 8000 --reload",
    "dev": "npx concurrently \"npm run dev:frontend\" \"npm run dev:backend\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
